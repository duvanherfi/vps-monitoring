# .kamal/secrets se commitea: solo tiene indirecciones VAR=$VAR, los valores
# vienen del entorno. Lo que nunca se sube son los ficheros con valores reales.
.kamal/secrets.*
.kamal/secrets-local

# ESTADO.md (la bitácora, con IP y dominios) SÍ se versiona aquí: éste es el
# repo privado. En la plantilla pública está ignorado, y ésa es la única
# diferencia intencionada entre ambos .gitignore.
